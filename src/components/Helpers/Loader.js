import React, { Component } from 'react';

import './Loader.css';

class Loader extends Component {
  /* Implements a loading spinner (with pure CSS) */
  render() {
    return(
      <div className="Loader"></div>
    );
  }
}

export default Loader;
